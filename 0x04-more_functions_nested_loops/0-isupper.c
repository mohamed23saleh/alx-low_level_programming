#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
int main(void)
{
        isUpper();
        return(0);
}
int isUpper(char c){
    if toupper(c) == c{
        return 1;
    }else {
    return 0;
    }
}
